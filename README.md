HTB Cap – Kurzprotokoll 1. Enumeration Nmap-Scan durchgeführt. Offene Ports identifiziert: 21 (FTP), 22 (SSH), 80 (HTTP). Fokus auf Web (Port 80). 2. Webanalyse Funktion „Security Snapshot“ entdeckt. PCAP-Dateien über URL erreichbar (/data/<id>). ID in URL manipuliert, Zugriff auf fremde Daten. Schwachstelle: IDOR. PCAP-Datei heruntergeladen (ID 0). 3. PCAP-Analyse Analyse mit Wireshark. Filter auf FTP-Traffic. FTP überträgt Daten im Klartext. Login-Daten (User + Passwort) extrahiert. 4. Initial Access Gefundene Credentials für SSH getestet. Login erfolgreich, Credential Reuse. Zugriff als User erhalten. User-Flag gelesen. 5. Privilege Escalation System nach Fehlkonfigurationen untersucht. getcap verwendet. Python-Binary mit cap_setuid gefunden. 6. Exploitation import os os.setuid(0) os.system("/bin/bash") Root-Rechte erlangt. Root-Flag gelesen.
